apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: http-error-rate-check
  namespace: dev
spec:
  metrics:
  - name: success-rate
    # Arka planda sürekli çalışmak yerine tek 1 kez ölçüm yapacak
    count: 1 
    successCondition: result[0] < 0.05
    # Eğer Prometheus'a bağlanamazsa bunu tolere etmeyip anında Error fırlatacak
    consecutiveErrorLimit: 0 
    provider:
      prometheus:
        address: http://prometheus-k8s.monitoring.svc.cluster.local:9090
        query: vector(0.08)
